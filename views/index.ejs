<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1 class="text-bg-success p-3">Blog Express</h1>
    <% for(post of posts){ %>
        <div class="card text-bg-primary mb-3" style="display: block">
            <h3 class="card-body">@<%= post.username %></h3>
            <h4><%= post.content %></h4>
        </div>
        <br>
        <a href="http://localhost:8080/posts/desc/<%= post.id %>">See In Detail</a>
        <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
        <form action="http://localhost:8080/posts/<%= post.id %>?_method=DELETE" method="post">
            <button>Delete Blog</button>
        </form>
    <% } %>
    <br>
    <a href="http://localhost:8080/posts/new/">Add New</a>
</body>
</html>